class-wp-ezclasses-buddypress-relocate-legacy-templates
=======================================================

Move your WordPress BuddyPress bp-legacy templates into a plugin for customizing (instead of trapping them in a theme).
